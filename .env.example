# =============================================================================
# Environment Variables for Portfolio Hub
# =============================================================================
# IMPORTANT: Nuxt requires the NUXT_ prefix for variables to be accessible
# via useRuntimeConfig(). The prefix is automatically stripped at runtime.
# Example: NUXT_DATABASE_URL -> runtimeConfig.databaseUrl
# =============================================================================

# Database Configuration (Railway PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
NUXT_DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# GitHub OAuth Application (nuxt-auth-utils convention)
# Create at: https://github.com/settings/applications/new
NUXT_OAUTH_GITHUB_CLIENT_ID=your_github_client_id
NUXT_OAUTH_GITHUB_CLIENT_SECRET=your_github_client_secret

# Session Configuration (min 32 characters)
# Generate: openssl rand -hex 32
NUXT_SESSION_PASSWORD=your_random_32_character_string_here

# Sentry Error Monitoring (Optional - errors logged only when DSN is set)
# Create project at: https://sentry.io
NUXT_PUBLIC_SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id

# Sentry Sourcemap Upload (Optional - for readable stack traces)
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your-sentry-auth-token
